# Shell scripts must keep LF endings even when checked out on Windows
# (core.autocrlf=true is set on the dev machine). CRLF in a .sh file makes
# Linux fail with "bad interpreter: /usr/bin/env bash^M".
*.sh text eol=lf
