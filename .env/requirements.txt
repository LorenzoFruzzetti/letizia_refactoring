numpy>=1.24
scipy>=1.10
pandas>=2.0
tifffile>=2023.7.10
matplotlib>=3.7
# roi_editor.py's interactive window (Qt is reached via `pyqtgraph.Qt`, so any of
# PySide6 / PyQt6 / PyQt5 / PySide2 works; pyside6 is what we test against).
pyqtgraph>=0.13
pyside6>=6.5
pyyaml>=6.0
pytest>=7.4
